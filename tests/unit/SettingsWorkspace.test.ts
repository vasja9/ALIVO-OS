import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
const html=fs.readFileSync("ui/index.html","utf8"),js=fs.readFileSync("ui/settings.js","utf8"),shell=fs.readFileSync("ui/shell.js","utf8"),css=fs.readFileSync("ui/styles.css","utf8"),contract=fs.readFileSync("src/application/settings/SettingsWorkspaceReadModel.ts","utf8"),surface=html+js+shell+css+contract;
test("Settings is first-class, defaults to General, supports search and deep links",()=>{assert.match(html,/id="settings-workspace"/);assert.match(shell,/alivo:settings:open/);assert.match(js,/category="general"/);assert.match(surface,/Search settings/);assert.match(shell,/data-settings-link/);for(const x of ["pinterest","blogs","integrations"])assert.match(html,new RegExp(`data-settings-link="${x}"`))});
test("Pinterest policy shows authoritative non-default cadence and governed update",()=>{for(const x of ["minimumPublicationIntervalMinutes:105","Minimum Publication Interval","UpdatePinterestConfiguration","expectedVersion","Review Changes","Future Pinterest scheduling","Schedule Rebalance Required","ValidationFailed","Succeeded","Failed","Conflict","AuthorizationDenied"])assert.match(surface,new RegExp(x));assert.match(js,/window\.alivoSettings\?\.command/)});
test("adaptive timing, timezone, recommendation and precise queue controls are present",()=>{for(const x of ["Timing Mode","Adaptive Timing","Adaptive Timezone","Manual Timezone","resolvedTimezone","Cadence Recommendation","Apply Recommendation","Target Queue Buffer","Minimum Queue Buffer","preferred number of production-ready Pins"])assert.match(surface,new RegExp(x,"i"))});
test("Blog promotion quota retains creative quality gate",()=>{for(const x of ["Blog → Pinterest Promotion","blogPinterestPromotion:true","Minimum Pins per Published Blog","Creative Diversity policy","low-quality duplicate Pins","CEO Approval Required","Every completed Blog Draft"])assert.match(surface,new RegExp(x,"i"));assert.doesNotMatch(surface,/VideoPin|videoDuration|videoPinsPerDay|Video Pins|Video per day/i)});
test("sources expose governed state, locale, health and authentication",()=>{for(const x of ["Research Sources","EnableSource","DisableSource","Capability","market","language","health","authenticationState","disableImpact"])assert.match(surface,new RegExp(x,"i"));assert.doesNotMatch(js,/CAPTCHA|proxy rotation|browser fingerprint|login-wall|hidden scraping/i)});
test("Business Package keeps identity, market and language separate with cross-package protection",()=>{for(const x of ["Immutable identifier","Human-readable name","Default markets","Default languages","Book Edition","ALIVO configuration cannot mutate BEST FINDS","cross-package response blocked"])assert.match(surface,new RegExp(x,"i"))});
test("publishing and integrations show references and status-only authentication navigation",()=>{for(const x of ["ALIVO WordPress","ALIVO Pinterest","Credential status only","Reauthorization Required","PermissionDenied","Configure","Open System health","openAuthentication"])assert.match(surface,new RegExp(x,"i"));assert.doesNotMatch(js,/password=|application_password=|api_key=|access_token=|refresh_token=|master_password=|Authorization header/i)});
test("Trust is summarized and routes to the shared editor",()=>{assert.match(surface,/Trust & Authority Summary/);assert.match(js,/#system\/trust/);assert.match(surface,/does not duplicate the Trust editor/)});
test("application preferences remain visibly local and save separately",()=>{for(const x of ["Local application preferences","Start workspace","Sidebar collapsed","Display density","Default performance period","saved separately from Business Package"])assert.match(surface,new RegExp(x,"i"))});
test("explicit grouped save handles unsaved changes, validation, impact, success and stale conflict",()=>{for(const x of ["Save Changes","unsaved changes","Backend validation response","Impact preview","Configuration updated","Stale configuration conflict","current configuration remains unchanged"])assert.match(surface,new RegExp(x,"i"));assert.match(js,/beforeunload/)});
test("configuration history is category-filtered and audit-readable",()=>{for(const x of ["Configuration History","Category filter","Old Value","New Value","Changed By","Timestamp","Reason","Existing Audit system","one-click rollback is not available"])assert.match(surface,new RegExp(x,"i"))});
test("settings use gateway commands and never direct persistence or unsafe editors",()=>{assert.match(contract,/SettingsWorkspaceGateway/);assert.match(contract,/SettingsWorkspaceReadModel/);assert.doesNotMatch(js,/fetch\(|XMLHttpRequest|localStorage|indexedDB|Repository|\.env|raw JSON|config.file|process\.env/i)});
