/** The controlled origins from which configuration may be registered. */
export enum ConfigurationSource {
  Default = "Default",
  Environment = "Environment",
  Local = "Local",
  Runtime = "Runtime",
}
