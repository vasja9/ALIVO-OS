import {BlogPublicationPackage,BlogPublicationPackageId,BlogPublicationPackagingException} from "./BlogPublicationDomain.ts";
export class BlogPublicationPackageRepository{readonly #items=new Map<string,BlogPublicationPackage[]>();save(value:BlogPublicationPackage){const history=this.#items.get(value.id.value)??[];if(history.some(x=>x.version.value===value.version.value))throw new BlogPublicationPackagingException("Publication Package version already exists","DUPLICATE_PACKAGE");if(history.length&&value.version.value!==history.at(-1)!.version.value+1)throw new BlogPublicationPackagingException("Publication Package must use the next immutable version","INVALID_VERSION");this.#items.set(value.id.value,[...history,value]);return value;}get(id:BlogPublicationPackageId,version?:number){const values=this.#items.get(id.value)??[];return version===undefined?values.at(-1):values.find(x=>x.version.value===version);}history(id:BlogPublicationPackageId){return Object.freeze([...(this.#items.get(id.value)??[])]);}}
