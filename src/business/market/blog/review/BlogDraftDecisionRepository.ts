import {BlogDraftDecisionRecord,BlogDraftReviewId} from "./BlogDraftReviewDomain.ts";import {BlogDraftReviewException} from "./BlogDraftReviewException.ts";
export class BlogDraftDecisionRepository {readonly #records=new Map<string,BlogDraftDecisionRecord[]>();store(record:BlogDraftDecisionRecord){const history=this.#records.get(record.reviewId.value)??[],same=history.find(x=>x.decisionId===record.decisionId);if(same){if(same.decisionType===record.decisionType&&same.note===record.note)return same;throw new BlogDraftReviewException("Decision identifier has conflicting content","DECISION_CONFLICT");}this.#records.set(record.reviewId.value,[...history,record]);return record;}history(id:BlogDraftReviewId){return Object.freeze([...(this.#records.get(id.value)??[])]);}latest(id:BlogDraftReviewId){return this.history(id).at(-1);}}
