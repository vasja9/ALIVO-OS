import {PinterestCreationPlan,PinterestCreationPlanId,PinterestCreationPlanningException,PinterestCreationPlanState} from "./PinterestCreationPlanningDomain.ts";
export class PinterestCreationPlanRepository{readonly #plans=new Map<string,PinterestCreationPlan[]>();store(plan:PinterestCreationPlan){const history=this.#plans.get(plan.id.value)??[];if(plan.version.value!==history.length+1)throw new PinterestCreationPlanningException("Plan versions must append immutable history","INVALID_VERSION");if(history.at(-1)?.state===PinterestCreationPlanState.ReadyForProduction&&plan.previousVersion!==history.at(-1)?.version.value)throw new PinterestCreationPlanningException("Material change must reference previous immutable version","PREVIOUS_VERSION_REQUIRED");this.#plans.set(plan.id.value,[...history,plan]);return plan;}current(id:PinterestCreationPlanId){return this.#plans.get(id.value)?.at(-1);}history(id:PinterestCreationPlanId){return Object.freeze([...(this.#plans.get(id.value)??[])]);} }
