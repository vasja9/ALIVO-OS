# Windows application identity

The Windows certification machine supplies the approved ALIVO OS icon as
`build-resources/icon.ico` before running the release build. The icon is a release
input managed by the certification process and is intentionally not represented
by a generated or placeholder binary in source control.
