const API_ROOT='https://api.pinterest.com/v5';
const ALLOWED_IMAGE_TYPES=new Set(['image/jpeg','image/png']);
function classify(status){if(status===401)return'Authentication Required';if(status===403)return'Permission Denied';if(status===429)return'Rate Limited';return status>=500?'Unavailable':'Provider Error';}
async function timedFetch(url,options={}){const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),20000);try{return await fetch(url,{...options,signal:controller.signal});}finally{clearTimeout(timeout);}}
async function api(accessToken,path,options={}){const response=await timedFetch(`${API_ROOT}${path}`,{...options,headers:{Authorization:`Bearer ${accessToken}`,Accept:'application/json',...(options.headers||{})}});let payload={};try{payload=await response.json();}catch{}if(!response.ok)return{error:{state:classify(response.status),statusCode:response.status,message:payload?.message||`Pinterest Production returned HTTP ${response.status}.`}};return{payload};}
function validate(input={}){const boardName=String(input.boardName||'').trim(),title=String(input.title||'').trim(),description=String(input.description||'').trim(),link=String(input.link||'').trim(),imageUrl=String(input.imageUrl||'').trim(),imageBase64=String(input.imageBase64||'').trim(),imageContentType=String(input.imageContentType||'').trim().toLowerCase(),altText=String(input.altText||'').trim();if(!boardName)return{error:'A Pinterest board name is required.'};if(!title||title.length>100)return{error:'Pin title is required and must be at most 100 characters.'};if(description.length>800)return{error:'Pin description must be at most 800 characters.'};if(altText.length>500)return{error:'Pin alt text must be at most 500 characters.'};if(imageBase64){if(!ALLOWED_IMAGE_TYPES.has(imageContentType))return{error:'Uploaded image must be JPEG or PNG.'};if(imageBase64.length>25_000_000)return{error:'Uploaded image is too large.'};}else{try{const u=new URL(imageUrl);if(u.protocol!=='https:')throw new Error();}catch{return{error:'A public HTTPS image URL or uploaded image is required.'};}}if(link){try{const u=new URL(link);if(!['https:','http:'].includes(u.protocol))throw new Error();}catch{return{error:'Pin destination link is invalid.'};}}return{value:{boardName,title,description,link,imageUrl,imageBase64,imageContentType,altText}};}
async function ensureBoard(accessToken,boardName){let bookmark;do{const params=new URLSearchParams({page_size:'100'});if(bookmark)params.set('bookmark',bookmark);const listed=await api(accessToken,`/boards?${params}`);if(listed.error)return listed;const match=(listed.payload?.items||[]).find(board=>String(board.name||'').trim().toLowerCase()===boardName.toLowerCase());if(match?.id)return{board:match,created:false};bookmark=listed.payload?.bookmark||undefined;}while(bookmark);const created=await api(accessToken,'/boards',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:boardName,privacy:'PUBLIC'})});if(created.error)return created;return{board:created.payload,created:true};}
function createPinterestProductionTrialTransport(getAccessToken){async function create(input={}){const checked=validate(input);if(checked.error)return{state:'Configuration Invalid',message:checked.error};const accessToken=typeof getAccessToken==='function'?await getAccessToken():undefined;if(!accessToken)return{state:'Authentication Required',message:'Pinterest Production token is not configured.'};const v=checked.value;try{const boardResult=await ensureBoard(accessToken,v.boardName);if(boardResult.error)return boardResult.error;const boardId=boardResult.board?.id;if(!boardId)return{state:'Provider Error',message:'Pinterest Production did not return a Board ID.'};const mediaSource=v.imageBase64?{source_type:'image_base64',is_standard:true,content_type:v.imageContentType,data:v.imageBase64}:{source_type:'image_url',url:v.imageUrl,is_standard:true};const body={board_id:boardId,title:v.title,description:v.description||undefined,link:v.link||undefined,alt_text:v.altText||undefined,media_source:mediaSource},created=await api(accessToken,'/pins',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(created.error)return created.error;const payload=created.payload;return{state:'Published',environment:'Production Trial',trial:true,pinId:payload?.id,boardId:payload?.board_id||boardId,boardName:v.boardName,boardCreated:boardResult.created,title:payload?.title||v.title,createdAt:payload?.created_at||new Date().toISOString(),link:payload?.link||v.link,message:'Pinterest controlled Production trial confirmed Pin creation.'};}catch(error){return{state:'Unavailable',message:error?.name==='AbortError'?'Pinterest Production trial Pin creation timed out.':'Pinterest Production trial Pin could not be created.'};}}return Object.freeze({create});}
module.exports={createPinterestProductionTrialTransport,ALLOWED_IMAGE_TYPES};