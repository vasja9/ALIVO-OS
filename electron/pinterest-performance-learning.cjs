const path=require('node:path');
const fs=require('node:fs/promises');

const PERFORMANCE_FILE='pinterest-performance.json';
const ACCOUNT_FILE='pinterest-account-analytics.json';
const MIN_IMPRESSIONS=25;
const MIN_SAMPLES_PER_BUCKET=2;
const CONFIDENCE_PRIOR_SAMPLES=8;
const CANDIDATE_HORIZON_DAYS=7;
const CANDIDATE_LIMIT=8;

function safeNumber(v){const n=Number(v);return Number.isFinite(n)?n:0;}
function readJson(file){return fs.readFile(file,'utf8').then(JSON.parse).catch(e=>{if(e?.code==='ENOENT')return null;throw e;});}
function metricScore(metrics={}){const impressions=Math.max(0,safeNumber(metrics.impressions)),saves=Math.max(0,safeNumber(metrics.saves)),pinClicks=Math.max(0,safeNumber(metrics.pinClicks)),outboundClicks=Math.max(0,safeNumber(metrics.outboundClicks));if(impressions<MIN_IMPRESSIONS)return null;return Number((((outboundClicks/impressions)*55)+((saves/impressions)*30)+((pinClicks/impressions)*15)).toFixed(6));}
function localParts(value){const d=new Date(value);if(Number.isNaN(d.getTime()))return null;return{hour:d.getHours(),day:d.getDay()};}
function aggregate(entries,keyFn,globalScore){const map=new Map();for(const e of entries){const key=keyFn(e);if(key===null||key===undefined)continue;const a=map.get(key)||{key,samples:0,scoreSum:0,impressions:0,outboundClicks:0,saves:0,pinClicks:0};a.samples++;a.scoreSum+=e.score;a.impressions+=e.impressions;a.outboundClicks+=e.outboundClicks;a.saves+=e.saves;a.pinClicks+=e.pinClicks;map.set(key,a);}return[...map.values()].map(a=>{const averageScore=a.scoreSum/a.samples,confidence=a.samples/(a.samples+CONFIDENCE_PRIOR_SAMPLES),confidenceScore=(averageScore*confidence)+(globalScore*(1-confidence));return{...a,averageScore:Number(averageScore.toFixed(6)),confidence:Number(confidence.toFixed(4)),confidenceScore:Number(confidenceScore.toFixed(6)),eligible:a.samples>=MIN_SAMPLES_PER_BUCKET};}).sort((a,b)=>Number(a.key)-Number(b.key));}
function normalizeLatestPerformance(history){const latest=history?.snapshots?.at?.(-1)||history?.snapshots?.[history.snapshots.length-1],pins=Array.isArray(latest?.pins)?latest.pins:[],entries=[];for(const pin of pins){const metrics=pin?.metrics?.lifetime||{},score=metricScore(metrics),parts=localParts(pin.createdAt);if(score===null||!parts)continue;entries.push({pinId:pin.id,title:pin.title||'',boardId:pin.boardId||null,createdAt:pin.createdAt,hour:parts.hour,day:parts.day,score,impressions:safeNumber(metrics.impressions),outboundClicks:safeNumber(metrics.outboundClicks),saves:safeNumber(metrics.saves),pinClicks:safeNumber(metrics.pinClicks)});}return{latest,entries};}
function accountTrend(history){const snapshots=Array.isArray(history?.snapshots)?history.snapshots:[],latestByWindow={};for(const s of snapshots){const days=Number(s?.windowDays);if([30,60,90].includes(days))latestByWindow[days]=s;}const thirty=latestByWindow[30];return{available:Boolean(thirty),windowDays:thirty?.windowDays||null,metrics:thirty?.metrics||null,collectedAt:thirty?.collectedAt||null};}
function learnedCandidates(hourly,weekday,from=new Date()){const hourMap=new Map(hourly.filter(x=>x.eligible).map(x=>[Number(x.key),x])),dayMap=new Map(weekday.filter(x=>x.eligible).map(x=>[Number(x.key),x])),now=new Date(from),candidates=[];if(Number.isNaN(now.getTime()))return candidates;for(let offset=0;offset<CANDIDATE_HORIZON_DAYS;offset++){const date=new Date(now);date.setDate(now.getDate()+offset);const day=dayMap.get(date.getDay());if(!day)continue;for(const hour of hourMap.values()){const slot=new Date(date);slot.setHours(Number(hour.key),0,0,0);if(slot<=now)continue;const combined=(hour.confidenceScore*0.65)+(day.confidenceScore*0.35),confidence=(hour.confidence*0.65)+(day.confidence*0.35);candidates.push({scheduledFor:slot.toISOString(),localHour:Number(hour.key),localDay:date.getDay(),score:Number(combined.toFixed(6)),confidence:Number(confidence.toFixed(4)),hourScore:hour.confidenceScore,dayScore:day.confidenceScore,hourSamples:hour.samples,daySamples:day.samples,reason:`Learned hour ${String(hour.key).padStart(2,'0')}:00 + weekday signal`});}}return candidates.sort((a,b)=>b.score-a.score||b.confidence-a.confidence||Date.parse(a.scheduledFor)-Date.parse(b.scheduledFor)).slice(0,CANDIDATE_LIMIT);}
function buildModel(performanceHistory,accountHistory,now=new Date()){const normalized=normalizeLatestPerformance(performanceHistory||{}),entries=normalized.entries,globalScore=entries.length?entries.reduce((sum,e)=>sum+e.score,0)/entries.length:0,hourly=aggregate(entries,e=>e.hour,globalScore),weekday=aggregate(entries,e=>e.day,globalScore),eligibleHours=hourly.filter(x=>x.eligible).sort((a,b)=>b.confidenceScore-a.confidenceScore||b.samples-a.samples),eligibleDays=weekday.filter(x=>x.eligible).sort((a,b)=>b.confidenceScore-a.confidenceScore||b.samples-a.samples),candidates=learnedCandidates(hourly,weekday,now);return{state:entries.length?'Learning':'Insufficient Data',mode:'READ_ONLY',generatedAt:new Date().toISOString(),sourceSnapshotAt:normalized.latest?.collectedAt||null,samplePins:entries.length,minImpressions:MIN_IMPRESSIONS,minSamplesPerBucket:MIN_SAMPLES_PER_BUCKET,confidencePriorSamples:CONFIDENCE_PRIOR_SAMPLES,globalScore:Number(globalScore.toFixed(6)),performanceSignals:entries.length>0,hourly,weekday,bestHours:eligibleHours.slice(0,5),bestDays:eligibleDays.slice(0,3),recommendedSlots:candidates,candidateHorizonDays:CANDIDATE_HORIZON_DAYS,accountTrend:accountTrend(accountHistory||{}),weights:{outboundClickRate:0.55,saveRate:0.30,pinClickRate:0.15,candidateHour:0.65,candidateDay:0.35},notes:['No scheduler decisions are changed by this model yet.','Recommended slots combine confidence-weighted hour and weekday signals.','Confidence weighting shrinks small-sample buckets toward the overall average.']};}
function createPinterestPerformanceLearning(app){const stateDir=path.join(app.getPath('userData'),'state');async function model(){const[performanceHistory,accountHistory]=await Promise.all([readJson(path.join(stateDir,PERFORMANCE_FILE)),readJson(path.join(stateDir,ACCOUNT_FILE))]);return buildModel(performanceHistory||{},accountHistory||{});}return Object.freeze({model});}
module.exports={createPinterestPerformanceLearning,buildModel,metricScore,learnedCandidates,MIN_IMPRESSIONS,MIN_SAMPLES_PER_BUCKET,CONFIDENCE_PRIOR_SAMPLES,CANDIDATE_HORIZON_DAYS};